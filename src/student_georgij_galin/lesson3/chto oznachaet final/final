In Java, final is a keyword. Which means, that once this variable has been assigned a value it cannot be changed.

В Java final является ключевым словом. Это означает, что когда этой переменной присвоено значение, ее нельзя изменить.