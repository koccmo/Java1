
Возможно ли в Java объявить класс и сделать его приватным,
не видимым для всего другого кода? Подумайте почему?
Ответ на этот вопрос закомитьте в текстовом файле.

@CodeReview(approved = true)

Приватный оператор доступа может быть только у внутреннего/вложенного класса.
В этом случае вложенный класс будет виден только в рамках главного класса.

Главный класс не может быть приватным т.к.
    1. теряется смысл объектно-ориентированного программирования
    2. компилятор выдаст ошибку